import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  /*assetPrefix: '/insighter-static',
  async rewrites() {
    return {
      beforeFiles: [
        {
          source: '/insighter-static/_next/:path+',
          destination: '/_next/:path+',
        }
      ],
      afterFiles: [],
      fallback: []
    }
  },*/
};

export default nextConfig;
